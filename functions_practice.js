//create a function that takes 2 #s as arguments and returns their sum

function doMath(num1, num2){
    return(num1 + num2);
}
const n1 = 75
const n2 = 25

    //console.log(doMath(n1,n2));

//write a function that takes an interger minutes and converts it to seconds

function convertTime(minutes){
    return(minutes * 60);
}
const minutes = 5

    //console.log(convertTime(minutes));

//write a function that takes the base and height of a triangle and returns its area

function findArea(base, height){
    return(base * height) /2;
}
const base = 7
const height = 12

    //console.log(findArea(base, height));

//create a function that takes age in years and returns the age in days

function calculateAgeInDays(years){
    return(years * 365);
}
const age = 22

    //console.log(calculateAgeInDays(age));

//create a function that takes an array containing numbers and return the first element

const numbers = [12,50,82,20,502,93]

function getNumber(index){
    return(numbers[index]);
}

    //console.log(getNumber(0));



